var e = require("typescript");

var i = require("@gql.tada/internal");

var n = require("@0no-co/graphqlsp/api");

var r = require("./index-chunk.js");

class GraphQLError extends Error {
  constructor(e, i, n, r, a, t, o) {
    super(e);
    this.name = "GraphQLError";
    this.message = e;
    if (a) {
      this.path = a;
    }
    if (i) {
      this.nodes = Array.isArray(i) ? i : [ i ];
    }
    if (n) {
      this.source = n;
    }
    if (r) {
      this.positions = r;
    }
    if (t) {
      this.originalError = t;
    }
    var l = o;
    if (!l && t) {
      var s = t.extensions;
      if (s && "object" == typeof s) {
        l = s;
      }
    }
    this.extensions = l || {};
  }
  toJSON() {
    return {
      ...this,
      message: this.message
    };
  }
  toString() {
    return this.message;
  }
  get [Symbol.toStringTag]() {
    return "GraphQLError";
  }
}

var a;

var t;

function error(e) {
  return new GraphQLError(`Syntax Error: Unexpected token at ${t} in ${e}`);
}

function advance(e) {
  e.lastIndex = t;
  if (e.test(a)) {
    return a.slice(t, t = e.lastIndex);
  }
}

var o = / +(?=[^\s])/y;

function blockString(e) {
  var i = e.split("\n");
  var n = "";
  var r = 0;
  var a = 0;
  var t = i.length - 1;
  for (var l = 0; l < i.length; l++) {
    o.lastIndex = 0;
    if (o.test(i[l])) {
      if (l && (!r || o.lastIndex < r)) {
        r = o.lastIndex;
      }
      a = a || l;
      t = l;
    }
  }
  for (var s = a; s <= t; s++) {
    if (s !== a) {
      n += "\n";
    }
    n += i[s].slice(r).replace(/\\"""/g, '"""');
  }
  return n;
}

function ignored() {
  for (var e = 0 | a.charCodeAt(t++); 9 === e || 10 === e || 13 === e || 32 === e || 35 === e || 44 === e || 65279 === e; e = 0 | a.charCodeAt(t++)) {
    if (35 === e) {
      while (10 !== (e = a.charCodeAt(t++)) && 13 !== e) {}
    }
  }
  t--;
}

var l = /[_A-Za-z]\w*/y;

var s = new RegExp("(?:(null|true|false)|\\$(" + l.source + ')|(-?\\d+)((?:\\.\\d+)?[eE][+-]?\\d+|\\.\\d+)?|("""(?:"""|(?:[\\s\\S]*?[^\\\\])"""))|("(?:"|[^\\r\\n]*?[^\\\\]"))|(' + l.source + "))", "y");

var u = function(e) {
  e[e.Const = 1] = "Const";
  e[e.Var = 2] = "Var";
  e[e.Int = 3] = "Int";
  e[e.Float = 4] = "Float";
  e[e.BlockString = 5] = "BlockString";
  e[e.String = 6] = "String";
  e[e.Enum = 7] = "Enum";
  return e;
}(u || {});

var d = /\\/g;

function value(e) {
  var i;
  var n;
  s.lastIndex = t;
  if (91 === a.charCodeAt(t)) {
    t++;
    ignored();
    var r = [];
    while (93 !== a.charCodeAt(t)) {
      r.push(value(e));
    }
    t++;
    ignored();
    return {
      kind: "ListValue",
      values: r
    };
  } else if (123 === a.charCodeAt(t)) {
    t++;
    ignored();
    var o = [];
    while (125 !== a.charCodeAt(t)) {
      if (null == (i = advance(l))) {
        throw error("ObjectField");
      }
      ignored();
      if (58 !== a.charCodeAt(t++)) {
        throw error("ObjectField");
      }
      ignored();
      o.push({
        kind: "ObjectField",
        name: {
          kind: "Name",
          value: i
        },
        value: value(e)
      });
    }
    t++;
    ignored();
    return {
      kind: "ObjectValue",
      fields: o
    };
  } else if (null != (n = s.exec(a))) {
    t = s.lastIndex;
    ignored();
    if (null != (i = n[u.Const])) {
      return "null" === i ? {
        kind: "NullValue"
      } : {
        kind: "BooleanValue",
        value: "true" === i
      };
    } else if (null != (i = n[u.Var])) {
      if (e) {
        throw error("Variable");
      } else {
        return {
          kind: "Variable",
          name: {
            kind: "Name",
            value: i
          }
        };
      }
    } else if (null != (i = n[u.Int])) {
      var c;
      if (null != (c = n[u.Float])) {
        return {
          kind: "FloatValue",
          value: i + c
        };
      } else {
        return {
          kind: "IntValue",
          value: i
        };
      }
    } else if (null != (i = n[u.BlockString])) {
      return {
        kind: "StringValue",
        value: blockString(i.slice(3, -3)),
        block: !0
      };
    } else if (null != (i = n[u.String])) {
      return {
        kind: "StringValue",
        value: d.test(i) ? JSON.parse(i) : i.slice(1, -1),
        block: !1
      };
    } else if (null != (i = n[u.Enum])) {
      return {
        kind: "EnumValue",
        value: i
      };
    }
  }
  throw error("Value");
}

function arguments_(e) {
  if (40 === a.charCodeAt(t)) {
    var i = [];
    t++;
    ignored();
    var n;
    do {
      if (null == (n = advance(l))) {
        throw error("Argument");
      }
      ignored();
      if (58 !== a.charCodeAt(t++)) {
        throw error("Argument");
      }
      ignored();
      i.push({
        kind: "Argument",
        name: {
          kind: "Name",
          value: n
        },
        value: value(e)
      });
    } while (41 !== a.charCodeAt(t));
    t++;
    ignored();
    return i;
  }
}

function directives(e) {
  if (64 === a.charCodeAt(t)) {
    var i = [];
    var n;
    do {
      t++;
      if (null == (n = advance(l))) {
        throw error("Directive");
      }
      ignored();
      i.push({
        kind: "Directive",
        name: {
          kind: "Name",
          value: n
        },
        arguments: arguments_(e)
      });
    } while (64 === a.charCodeAt(t));
    return i;
  }
}

function type() {
  var e;
  var i = 0;
  while (91 === a.charCodeAt(t)) {
    i++;
    t++;
    ignored();
  }
  if (null == (e = advance(l))) {
    throw error("NamedType");
  }
  ignored();
  var n = {
    kind: "NamedType",
    name: {
      kind: "Name",
      value: e
    }
  };
  do {
    if (33 === a.charCodeAt(t)) {
      t++;
      ignored();
      n = {
        kind: "NonNullType",
        type: n
      };
    }
    if (i) {
      if (93 !== a.charCodeAt(t++)) {
        throw error("NamedType");
      }
      ignored();
      n = {
        kind: "ListType",
        type: n
      };
    }
  } while (i--);
  return n;
}

var c = new RegExp("(?:(\\.{3})|(" + l.source + "))", "y");

var v = function(e) {
  e[e.Spread = 1] = "Spread";
  e[e.Name = 2] = "Name";
  return e;
}(v || {});

function selectionSet() {
  var e = [];
  var i;
  var n;
  do {
    c.lastIndex = t;
    if (null != (n = c.exec(a))) {
      t = c.lastIndex;
      if (null != n[v.Spread]) {
        ignored();
        var r = advance(l);
        if (null != r && "on" !== r) {
          ignored();
          e.push({
            kind: "FragmentSpread",
            name: {
              kind: "Name",
              value: r
            },
            directives: directives(!1)
          });
        } else {
          ignored();
          if ("on" === r) {
            if (null == (r = advance(l))) {
              throw error("NamedType");
            }
            ignored();
          }
          var o = directives(!1);
          if (123 !== a.charCodeAt(t++)) {
            throw error("InlineFragment");
          }
          ignored();
          e.push({
            kind: "InlineFragment",
            typeCondition: r ? {
              kind: "NamedType",
              name: {
                kind: "Name",
                value: r
              }
            } : void 0,
            directives: o,
            selectionSet: selectionSet()
          });
        }
      } else if (null != (i = n[v.Name])) {
        var s = void 0;
        ignored();
        if (58 === a.charCodeAt(t)) {
          t++;
          ignored();
          s = i;
          if (null == (i = advance(l))) {
            throw error("Field");
          }
          ignored();
        }
        var u = arguments_(!1);
        ignored();
        var d = directives(!1);
        var f = void 0;
        if (123 === a.charCodeAt(t)) {
          t++;
          ignored();
          f = selectionSet();
        }
        e.push({
          kind: "Field",
          alias: s ? {
            kind: "Name",
            value: s
          } : void 0,
          name: {
            kind: "Name",
            value: i
          },
          arguments: u,
          directives: d,
          selectionSet: f
        });
      }
    } else {
      throw error("SelectionSet");
    }
  } while (125 !== a.charCodeAt(t));
  t++;
  ignored();
  return {
    kind: "SelectionSet",
    selections: e
  };
}

function fragmentDefinition() {
  var e;
  var i;
  if (null == (e = advance(l))) {
    throw error("FragmentDefinition");
  }
  ignored();
  if ("on" !== advance(l)) {
    throw error("FragmentDefinition");
  }
  ignored();
  if (null == (i = advance(l))) {
    throw error("FragmentDefinition");
  }
  ignored();
  var n = directives(!1);
  if (123 !== a.charCodeAt(t++)) {
    throw error("FragmentDefinition");
  }
  ignored();
  return {
    kind: "FragmentDefinition",
    name: {
      kind: "Name",
      value: e
    },
    typeCondition: {
      kind: "NamedType",
      name: {
        kind: "Name",
        value: i
      }
    },
    directives: n,
    selectionSet: selectionSet()
  };
}

var f = /(?:query|mutation|subscription|fragment)/y;

function operationDefinition(e) {
  var i;
  var n;
  var r;
  if (e) {
    ignored();
    i = advance(l);
    n = function variableDefinitions() {
      ignored();
      if (40 === a.charCodeAt(t)) {
        var e = [];
        t++;
        ignored();
        var i;
        do {
          if (36 !== a.charCodeAt(t++)) {
            throw error("Variable");
          }
          if (null == (i = advance(l))) {
            throw error("Variable");
          }
          ignored();
          if (58 !== a.charCodeAt(t++)) {
            throw error("VariableDefinition");
          }
          ignored();
          var n = type();
          var r = void 0;
          if (61 === a.charCodeAt(t)) {
            t++;
            ignored();
            r = value(!0);
          }
          ignored();
          e.push({
            kind: "VariableDefinition",
            variable: {
              kind: "Variable",
              name: {
                kind: "Name",
                value: i
              }
            },
            type: n,
            defaultValue: r,
            directives: directives(!0)
          });
        } while (41 !== a.charCodeAt(t));
        t++;
        ignored();
        return e;
      }
    }();
    r = directives(!1);
  }
  if (123 === a.charCodeAt(t)) {
    t++;
    ignored();
    return {
      kind: "OperationDefinition",
      operation: e || "query",
      name: i ? {
        kind: "Name",
        value: i
      } : void 0,
      variableDefinitions: n,
      directives: r,
      selectionSet: selectionSet()
    };
  }
}

function parse(e, i) {
  a = "string" == typeof e.body ? e.body : e;
  t = 0;
  return function document() {
    var e;
    var i;
    ignored();
    var n = [];
    do {
      if ("fragment" === (e = advance(f))) {
        ignored();
        n.push(fragmentDefinition());
      } else if (null != (i = operationDefinition(e))) {
        n.push(i);
      } else {
        throw error("Document");
      }
    } while (t < a.length);
    return {
      kind: "Document",
      definitions: n
    };
  }();
}

function mapJoin(e, i, n) {
  var r = "";
  for (var a = 0; a < e.length; a++) {
    if (a) {
      r += i;
    }
    r += n(e[a]);
  }
  return r;
}

var g = "\n";

var m = {
  OperationDefinition(e) {
    var i = e.operation;
    if (e.name) {
      i += " " + e.name.value;
    }
    if (e.variableDefinitions && e.variableDefinitions.length) {
      if (!e.name) {
        i += " ";
      }
      i += "(" + mapJoin(e.variableDefinitions, ", ", m.VariableDefinition) + ")";
    }
    if (e.directives && e.directives.length) {
      i += " " + mapJoin(e.directives, " ", m.Directive);
    }
    return "query" !== i ? i + " " + m.SelectionSet(e.selectionSet) : m.SelectionSet(e.selectionSet);
  },
  VariableDefinition(e) {
    var i = m.Variable(e.variable) + ": " + _print(e.type);
    if (e.defaultValue) {
      i += " = " + _print(e.defaultValue);
    }
    if (e.directives && e.directives.length) {
      i += " " + mapJoin(e.directives, " ", m.Directive);
    }
    return i;
  },
  Field(e) {
    var i = e.alias ? e.alias.value + ": " + e.name.value : e.name.value;
    if (e.arguments && e.arguments.length) {
      var n = mapJoin(e.arguments, ", ", m.Argument);
      if (i.length + n.length + 2 > 80) {
        i += "(" + (g += "  ") + mapJoin(e.arguments, g, m.Argument) + (g = g.slice(0, -2)) + ")";
      } else {
        i += "(" + n + ")";
      }
    }
    if (e.directives && e.directives.length) {
      i += " " + mapJoin(e.directives, " ", m.Directive);
    }
    if (e.selectionSet) {
      i += " " + m.SelectionSet(e.selectionSet);
    }
    return i;
  },
  StringValue(e) {
    if (e.block) {
      return function printBlockString(e) {
        return '"""\n' + e.replace(/"""/g, '\\"""') + '\n"""';
      }(e.value).replace(/\n/g, g);
    } else {
      return function printString(e) {
        return JSON.stringify(e);
      }(e.value);
    }
  },
  BooleanValue: e => "" + e.value,
  NullValue: e => "null",
  IntValue: e => e.value,
  FloatValue: e => e.value,
  EnumValue: e => e.value,
  Name: e => e.value,
  Variable: e => "$" + e.name.value,
  ListValue: e => "[" + mapJoin(e.values, ", ", _print) + "]",
  ObjectValue: e => "{" + mapJoin(e.fields, ", ", m.ObjectField) + "}",
  ObjectField: e => e.name.value + ": " + _print(e.value),
  Document(e) {
    if (!e.definitions || !e.definitions.length) {
      return "";
    }
    return mapJoin(e.definitions, "\n\n", _print);
  },
  SelectionSet: e => "{" + (g += "  ") + mapJoin(e.selections, g, _print) + (g = g.slice(0, -2)) + "}",
  Argument: e => e.name.value + ": " + _print(e.value),
  FragmentSpread(e) {
    var i = "..." + e.name.value;
    if (e.directives && e.directives.length) {
      i += " " + mapJoin(e.directives, " ", m.Directive);
    }
    return i;
  },
  InlineFragment(e) {
    var i = "...";
    if (e.typeCondition) {
      i += " on " + e.typeCondition.name.value;
    }
    if (e.directives && e.directives.length) {
      i += " " + mapJoin(e.directives, " ", m.Directive);
    }
    return i + " " + m.SelectionSet(e.selectionSet);
  },
  FragmentDefinition(e) {
    var i = "fragment " + e.name.value;
    i += " on " + e.typeCondition.name.value;
    if (e.directives && e.directives.length) {
      i += " " + mapJoin(e.directives, " ", m.Directive);
    }
    return i + " " + m.SelectionSet(e.selectionSet);
  },
  Directive(e) {
    var i = "@" + e.name.value;
    if (e.arguments && e.arguments.length) {
      i += "(" + mapJoin(e.arguments, ", ", m.Argument) + ")";
    }
    return i;
  },
  NamedType: e => e.name.value,
  ListType: e => "[" + _print(e.type) + "]",
  NonNullType: e => _print(e.type) + "!"
};

var _print = e => m[e.kind](e);

function print(e) {
  g = "\n";
  return m[e.kind] ? m[e.kind](e) : "";
}

var h = r.expose((async function* _runPersisted(a) {
  var t = i.getSchemaNamesFromConfig(a.pluginConfig);
  var o = r.programFactory(a);
  var l = o.createExternalFiles();
  if (l.length) {
    yield {
      kind: "EXTERNAL_WARNING"
    };
    await o.addVirtualFiles(l);
  }
  var s = o.build();
  var u = s.buildPluginInfo(a.pluginConfig);
  var d = s.getSourceFiles();
  yield {
    kind: "FILE_COUNT",
    fileCount: d.length
  };
  for (var c of d) {
    var v = c.fileName;
    var f = [];
    var g = [];
    var m = n.findAllPersistedCallExpressions(c, u);
    for (var h of m) {
      var p = s.getSourcePosition(c, h.node.getStart());
      v = p.fileName;
      if (!t.has(h.schema)) {
        g.push({
          message: h.schema ? `The '${h.schema}' schema is not in the configuration but was referenced by "graphql.persisted".` : t.size > 1 ? "The document is not for a known schema. Have you re-generated the output file?" : "Multiple schemas are configured, but the document is not for a specific schema.",
          file: p.fileName,
          line: p.line,
          col: p.col
        });
        continue;
      }
      var S = h.node.arguments[0];
      var y = h.node.arguments[1];
      var k = h.node.typeArguments && h.node.typeArguments[0];
      if (!S || !e.isStringLiteral(S)) {
        g.push({
          message: '"graphql.persisted" must be called with a string literal as the first argument.',
          file: p.fileName,
          line: p.line,
          col: p.col
        });
        continue;
      } else if (!y && !k) {
        g.push({
          message: '"graphql.persisted" is missing a document.\nThis may be passed as a generic such as `graphql.persisted<typeof document>` or as the second argument.',
          file: p.fileName,
          line: p.line,
          col: p.col
        });
        continue;
      }
      var N = null;
      var b = h.node;
      if (y && (e.isCallExpression(y) || e.isIdentifier(y))) {
        N = n.getDocumentReferenceFromDocumentNode(y, c.fileName, u).node;
        b = y;
      } else if (k && e.isTypeQueryNode(k)) {
        N = n.getDocumentReferenceFromTypeQuery(k, c.fileName, u).node;
        b = k;
      }
      if (!N) {
        g.push({
          message: `Could not find reference for "${b.getText()}".\nIf this is unexpected, please file an issue describing your case.`,
          file: p.fileName,
          line: p.line,
          col: p.col
        });
        continue;
      }
      if (!N || !e.isCallExpression(N) || !e.isNoSubstitutionTemplateLiteral(N.arguments[0]) && !e.isStringLiteral(N.arguments[0])) {
        g.push({
          message: `The referenced document of "${b.getText()}" contains no document string literal.\nIf this is unexpected, please file an issue describing your case.`,
          file: p.fileName,
          line: p.line,
          col: p.col
        });
        continue;
      }
      var C = [];
      var A = N.arguments[0].getText().slice(1, -1);
      if (N.arguments[1] && e.isArrayLiteralExpression(N.arguments[1])) {
        n.unrollTadaFragments(N.arguments[1], C, s.buildPluginInfo(a.pluginConfig));
      }
      var w = new Set;
      var D = void 0;
      if (a.disableNormalization) {
        D = A;
      } else {
        try {
          D = print(parse(A));
        } catch (e) {
          g.push({
            message: `The referenced document of "${b.getText()}" could not be parsed.\nRun \`check\` to see specific validation errors.`,
            file: p.fileName,
            line: p.line,
            col: p.col
          });
          continue;
        }
      }
      for (var V of C) {
        var F = print(V);
        if (!w.has(F)) {
          D += "\n\n" + print(V);
          w.add(F);
        }
      }
      f.push({
        schemaName: h.schema,
        hashKey: S.getText().slice(1, -1),
        document: D
      });
    }
    yield {
      kind: "FILE_PERSISTED",
      filePath: v,
      documents: f,
      warnings: g
    };
  }
}));

exports.runPersisted = h;
//# sourceMappingURL=thread-chunk.js.map
